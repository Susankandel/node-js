// const blogs = require("./my-own")

//Destructure
const { generateDescription } = require("./my-own")

blogs.generateDescription("susan is sowtware enginner");
console.log(result);

//Create a nord module for calculating the 
// area of cuboid 
// and import it to main.js file

const { calculateCuboidAread } = require("./cuboid");
const area = calculateCuboidAread(5, 6, 7);
console.log(area);